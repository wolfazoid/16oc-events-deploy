{
  "name": "16oc-events-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for 16OC venue Ticketmaster events",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
